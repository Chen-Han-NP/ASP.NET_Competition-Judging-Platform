@model IEnumerable<GameTime.Models.AreaOfInterest>

@{
    ViewData["Title"] = "Index";
}

<h1>Area Of Interests</h1>

<p>
    <a asp-action="CreateAOI">Create New Area Of Interest</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AreaInterestID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.AreaInterestID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @*@Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |*@
                    @Html.ActionLink("Delete", "Delete", new { id = item.AreaInterestID })
                </td>
            </tr>
            
        }
    </tbody>
</table>
<div>
    <a asp-controller="Home" asp-action="Admin">Back to Admin Page</a>
</div>